#!/bin/csh
# ============================================================
# run_uvm_simulation
# Sets up environment and launches the neural network UVM sim.
# Run this script from the sim/ directory.
# ============================================================

source /vol/eecs392/env/questasim.env

mkdir -p lib
make -f Makefile.questa dpi_lib32 LIBDIR=lib

vsim -do neural_net_sim.do
